// Section 8 - Coding Exercise
// Logical Operators and Operator Precedence

/*
    Determine job eligibility
    Parameters:
        must be 18 or older OR above 15 with parental consent
        must have ssn
        must not have accidents
 */

#include <iostream>

using namespace std;

void logical_operators(int age, bool parental_consent, bool ssn, bool accidents) {

    // my code below
    
    if ((age >= 18 || (age > 15 && parental_consent == true)) && ssn == true && accidents == false) // my code in parenthesis
        cout << "Yes, you can work.";
        
    return;
}
