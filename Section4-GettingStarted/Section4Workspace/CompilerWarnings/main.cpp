#include <iostream>

int main() {
    
    int favorite_number;
    
    std::cout << favorite_number << std::endl;  // compiler warns that favorite_number has not had a value assigned to it yet (hint: it won't necessarily be 0)
    return 0;                                   
}